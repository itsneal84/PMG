<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.cityofglasgowcollege.assessement3_pmg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pmg_launcher"
        android:label="People Make Glasgow"
        android:roundIcon="@mipmap/pmg_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".TripActivity" />
        <activity android:name=".eventsActivity" />
        <activity android:name=".VisitingActivity" />
        <activity android:name=".ThingsToDoActivity" />
        <activity android:name=".AddCommentActivity"></activity>
    </application>

</manifest>